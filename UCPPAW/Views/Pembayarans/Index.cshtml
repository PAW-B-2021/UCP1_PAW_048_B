@model IEnumerable<UCPPAW.Models.Pembayaran>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayName("TanggalBayar")
            </th>
            <th>
                @Html.DisplayName("IdTransaks")
            </th>
            <th>
                @Html.DisplayName("TotalBayarNavigation")
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.TanggalBayar)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IdTransaksi)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.TotalBayarNavigation.IdTransaksi)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdPembayaran"><i class="fa fa-marker fa-lg"></a> |
                <a asp-action="Details" asp-route-id="@item.IdPembayaran"><i class="fas fa-list"></i></a> |
                <a asp-action="Delete" asp-route-id="@item.IdPembayaran"><i class="fas fa-trash-alt fa-lg"></a>
            </td>
        </tr>
}
    </tbody>
</table>
